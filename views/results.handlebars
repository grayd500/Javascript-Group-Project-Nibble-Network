{{!-- views/results.handlebars: --}}
{{> navbar }}

<div class="container">
    <h2>Search Results</h2>
    <div class="list-group">
        {{#each searchResults}}
            <a href="/recipe/{{this.id}}" class="list-group-item list-group-item-action">{{this.title}}</a>
        {{/each}}
    </div>
</div>

